import { Button } from "@/components/ui/button";

const Home = () => {
  return (
    <div className="flex flex-col home-wrapper h-screen">
      Home
      <Button className="theme-btn">Click Me!</Button>
    </div>
  );
};

export default Home;
